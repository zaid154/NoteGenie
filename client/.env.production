# Production API (Render backend). /api path zaroori hai.
# Render service ka naam alag ho to yahan URL update karo, phir: npm run build && firebase deploy
VITE_API_URL=https://notegenie-api.onrender.com/api
